services:
  - type: web
    name: multimodal-gaming-ai-chat-backend
    env: python
    buildCommand: pip install -r backend/src/requirements.txt
    startCommand: uvicorn src.main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: PORT
        value: 10000
